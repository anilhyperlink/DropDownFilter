@model PaginationModel

<!-- Modal css -->
<link href="~/assets/plugins/custombox/dist/custombox.min.css" rel="stylesheet">

<table class="table table-striped m-b-0 table-hover toggle-circle">
    <thead>
        <tr>
            <th>#</th>
            <th>Profile<i class="fa fa-sort" aria-hidden="true"></i></th>
            <th>Name <i class="fa fa-sort" aria-hidden="true"></i></th>
            <th>Gender <i class="fa fa-sort" aria-hidden="true"></i></th>
            <th>Email <i class="fa fa-sort" aria-hidden="true"></i></th>
            <th>Mobile <i class="fa fa-sort" aria-hidden="true"></i></th>
            <th class="text-center">CreatedDate</th>
        </tr>
    </thead>
    @if (Model != null)
    {
        for (int i = 0; i < Model.UserList.Count; i++)
        {
            <tbody>
                <tr>
                    <td>@Model.UserList[i].UserId</td>
                    <td>@Model.UserList[i].ProfileImage</td>
                    <td>@(Model.UserList[i].FirstName + " " + Model.UserList[i].LastName) </td>
                    <td>@Model.UserList[i].Gender</td>
                    <td>@Model.UserList[i].EmailAddress</td>
                    <td>@Model.UserList[i].MobileNo</td>
                    <td class="text-center">@(Model.UserList[i].CreatedDate.Value.ToString("dd MMM yyyy"))</td>
                </tr>
            </tbody>
        }
        <tfoot>
            <tr>
                <td colspan="10">
                    <div class="row">

                        <div class="col-md-6">
                            <div>
                                <span class="pagination pagination-split m-t-30"> Showing <b> @Model.Pagination.CurrentPage </b> of <b>@Model.Pagination.TotalPages </b> pages</span>
                            </div>
                        </div>

                        <div class="col-md-6">
                            <div class="text-right">
                                <ul class="pagination pagination-split m-t-30">

                                    @if (!Model.Pagination.ShowPrevious)
                                    {
                                        <li class="disabled">
                                            <a><b>‹</b></a>
                                        </li>
                                    }
                                    else
                                    {
                                        <li>
                                            <a href="javascript:void(0);" onclick="ChangePage(@Model.Pagination.CurrentPage - 1)"><b>‹</b></a>
                                        </li>
                                    }

                                    <li><a href="javascript:void(0);"><b>@Model.Pagination.CurrentPage</b></a></li>

                                    @if (!Model.Pagination.ShowNext)
                                    {
                                        <li class="disabled">
                                            <a class="disabled"><b>›</b></a>
                                        </li>
                                    }
                                    else
                                    {
                                        <li>
                                            <a href="javascript:void(0);" onclick="ChangePage(@Model.Pagination.CurrentPage + 1)"><b>›</b></a>
                                        </li>
                                    }
                                </ul>
                            </div>
                        </div>
                    </div>
                </td>
            </tr>
        </tfoot>
    }
</table>

